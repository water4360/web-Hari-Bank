<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <!-- 앞의 패키지 3개는 뗐음~~  -->
<mapper namespace="comment.dao.CommentDAO">
	
	<insert id="insert" parameterType="commentVO">
		INSERT INTO T_COMMENT(NO, BOARD_NO, CONTENT, WRITER)
			VALUES(SEQ_COMMENT_NO.NEXTVAL, #{boardNo}, #{content}, #{writer})
	</insert>

	<select id="selectAll" parameterType="int" resultType="commentVO">
		SELECT NO, BOARD_NO boardNo, CONTENT, WRITER
        , TO_CHAR(REG_DATE, 'yyyy-mm-dd') as REGDATE
      	FROM T_COMMENT WHERE BOARD_NO = #{boardNo}
		ORDER BY NO DESC
	</select>
	

</mapper>



